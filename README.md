# Multi_Recon
Sequential recon automation script for a target host/domain.  The script runs multiple common recon tools one after another and saves output to timestamped files in a results directory.
